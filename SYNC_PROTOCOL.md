# 🍓 Протокол Синхронизации KLUB

Этот проект живет в двух мирах: 
1. **Админка** (обновляет `products.json` на GitHub).
2. **Локальный код** (мы правим его на компе).

Чтобы они не ссорились, следуй этим правилам:

## 1. ПЕРЕД началом работы
Всегда делай «глоток свежего кода»:
```powershell
git pull origin main
```
Это скачает все новые цены и товары, которые ты поменял через телефон в админке.

## 2. ПРИ ошибке "Rejected" (Конфликт)
Если ты попытался сделать `git push`, но получил ошибку `rejected`, введи «Комбо-Удар»:
```powershell
git pull --rebase origin main
git push origin main
```
**Что это делает:** Гит скачает изменения из админки, "поднимет" твои новые строчки кода и положит их сверху. Ничего не сотрется!

## 3. ВАЖНО
Никогда не используй `git push --force`, иначе мы сотрем все товары из админки!

---
*Сделано с любовью для Апельсинки 🍓*
